<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/background</item>
        <item name="colorControlActivated">@color/lightSalomon</item>
        <item name="colorControlHighlight">@color/lightSalomon</item>
        <item name="colorControlNormal">@color/lightSalomon</item>
        <item name="android:textColorPrimary">@color/lightSalomon</item>
    </style>

</resources>
